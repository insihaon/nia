package com.ipsdn_opt.app.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.ipsdn_opt.app.model.RoutePathSet;

public interface RoutePathSetRepository extends JpaRepository<RoutePathSet, Long> {
    
}
